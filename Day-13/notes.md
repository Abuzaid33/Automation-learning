🧠 Warm-up Questions (Answer Without Google)

Add these to notes.md before coding:

Why do companies prefer CSV over Excel for automation?
Because its easy fast and less messy also its lite 
What does "Comma Separated Values" actually mean?
its means that the values are seprated by commas
What is a header row?
its represent the coloums like the name rollno ids etc
What is a record?
A record refers to a complete row in context of csv its basicaaly a set of information about something
What is a delimiter?
delimeter is a seprator which seprate the values in csv its comma
How is CSV different from JSON?
csv is in comma seprated format while json is in key value pair also json is used for APIS while csv is better for tabels
Can CSV store colors or formulas?
yes it can store everything but in term of values 
Give three real-world examples where CSV is used.
like in students report cards in employee cards generation and salries genration


🧠 Think Like an Engineer

Suppose tomorrow your company sends you a CSV file with 2 million employees.

Would you:

Option A

Read the entire file into one huge string and manually split it.

OR

Option B

Use csv.reader() and process one row at a time.

Explain why in your notes.md.

Option 2 because it smore convinient and reliable also its uses less processing its fast and easy


📚 New Technical Terms (Add to notes.md)

Write these definitions in your own words:

CSV Module
Its a python module that is used to handle csv operation such as reading and writing csv files
csv.writer()
Writes the data in csv files
csv.reader()
reads the content of csv files
writer.writerow()
write an entire row in csv files
Header Row
represent coloums of the csv file or heading of the records
Record
an entire row is called a record in context of csv files which holds the data 
Row
left tp right in horizontal format also known as record
Column
top to bottom in vertical format also known as header
Delimiter
The seprator which seprates the values such as comma
newline=""
skip blanks after every line
next()
skips the header or coloums 
Iteration
One time runnig of the loop and reads the record