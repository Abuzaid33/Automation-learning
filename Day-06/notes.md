🧠 Warm-up Quiz (Don't Google)

Answer these in notes.md.

1. What is a function?
A function is a reuseable block of code that is used to perform a specific task it is used whenever called
2. Why do programmers use functions?
to reduce redundancy ,make code shorter saves time makes code readable and easy to maintain
3. What is code reusability?
the ability of the code to use it again and again
4. What is modular programming?
break down the code in form of blocks or module so we can access them easily also they are super easy to manage and maintain
5. Can a function return data?
yes a function can return data or something we want it to return acctually a functions basic work is to return somethnig not to print something 


📚 New Technical Terms

Write definitions in your own words:

Function
A function is a reuseable block of code that is used to perform a specific task it is used whenever called
Parameter
the variable used in function head
Argument
the actual value used in function calling
Return
its a function that return the data back to caller
Reusability
the ability of the code to use it again and again
Modular Programming
break down the code in form of blocks or module so we can access them easily also they are super easy to manage and maintain
Caller
the one who called the function
Callee
the function that is called
Scope (Introduction only—we'll explore it deeply later.)
scope tells us about the variable usage inside or outside the funcation there are two types of scopes global and local the global scope is when a variabl eis initalized outside th function and it can be used everywhee in program while the local scope is only used inside th function but we can make a local scope variable global by global keyword