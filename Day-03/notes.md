🧠 Warm-Up Quiz

Answer these in notes.md before coding:

Why do companies store data in files?
Companies store data in files because different data has different types such as pdf images doc and every data should be treated differently files have diffent formats for everykind of data thats why companies stores data in files also when the files data is extracted it can be fetched in binary form which is understandable by the computer and more fast also the files holds data permanently.
What happens if a file doesn't exist?
if a file dosent exist it raise an error file not found
Why is writing logs important?
writing logs is important because it saves a record of operation happened and when
What's the difference between reading and writing a file?
reading files is used to read and extract the content of the files while writing create and write new data in file if teh data is already exists it overwrite the data 
Can software create a file automatically?
yes a software is capable of creating a file automatically.