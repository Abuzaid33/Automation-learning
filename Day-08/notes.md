🧠 Warm-up Quiz (Don't Google)

Answer these in notes.md.

What is Object-Oriented Programming (OOP)?
OOP is a type of programing where everything is dealed like a object.
What is a class?
a class in oop acts like a prototype for the objects e.g if we try to add students we have a class student
What is an object?
an object is the decendent or instance of the class which has the properties of the class as well as its own
What is the difference between a function and a method?
A function is a block of code used to perform a specific task and can be reusable while a method is also a function but is already builtin language
Why do large software companies use OOP?
because its easier to manage and maintain objects and we can use a prototype class properties multiple times also its used in database.

📚 New Technical Terms

Write definitions in your own words.

OOP
OOP is a type of programing where everything is dealed like a object.
Class
a class in oop acts like a prototype for the objects e.g if we try to add students we have a class student
Object
an object is the decendent or instance of the class which has the properties of the class as well as its own
Constructor
A constructor is a method inside a clas used to create object.
__init__
Its an specail constructor methode used to create object instances and add properties to the object
self
self refers to the curent instance of the class
Method
the function that belongs to the class
Instance
the object which is curently called
Instance Variable
the variable which is curently called
Blueprint
the class from which the object is created
Encapsulation (Introduction only)
?



🧠 Think Like an Engineer

Suppose tomorrow your company hires 20,000 new employees.

the option b is used because it reduces the code make the code more cleaner more easier to manage and maintain.

👨‍🏫 Mentor Challenge (Design Question #7)
i would create seprate classes for all the tasks beacause its not related to the employee class and also it creates a mess 