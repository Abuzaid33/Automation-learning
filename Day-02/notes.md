🧠 Warm-Up Quiz (Don't Google)

Answer these questions in notes.md:

What is an Operating System?
An operating system is a system software that is used to manage and maintain the hardware and application softwares of a computer system
Why does Python need the os module?
Python needs aOS module because it is required to communicate with the operating system to handle file operations such as creating deleting etc
What is the difference between a file and a directory?
A directory is a folder which ast like a container for the files while a file can be something like an image , video , pdf etc determined by its extension
Can Python create folders without os?
no
Why do automation engineers work with the file system?
An automation engineer work with the file system to automate file handling tasks such as creation deletion etc




📚 New Technical Terms

Add these to notes.md:

Operating System (OS)
An operating system is a system software that is used to manage and maintain the hardware and application softwares of a computer system
Module
A module is a collection of group of relative function that is used to solve a specific problem or to reduce the redundancy in code 
Function
a function is a block of code that is used to perform a specific task
Current Working Directory (CWD)
Path
a path is like a address its tells the location of the file or folder
Absolute Path
absolut epath is a complete address from root folder to destination file
Relative Path
relative path is the address of the folder which we are curently in 
File System
a file system is a system that is used to manage and maintain the files and do the file operation like removing copying moving etc
Directory
a directry is a folder we store our files in

🧠 Think Like an Engineer

You're working at a company.

The company receives 20,000 files every day.

Before moving any file, what's the first thing your automation should do?

Think carefully.

(Hint: You can't organize files you haven't discovered yet.)

Before moving the files The work of automation is simple it should recognize the files type by using the is file or is folder function .