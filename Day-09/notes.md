🧠 Warm-up Quiz (Don't Google)

Answer these in notes.md.

What is a Python module?
A python module is also called a library which is used for specific operation such as os module it is used to communicate with operation system .It is basically set of instructionn that we need again and again and also so we can not code it multiple times eg if a function is depending on 1000 lines we should create a module of it so we can use it again and again rather than writing its code again and again
Why do developers split projects into multiple files?
Developers split projects into multiple files to make it clean and easy to maintain so a mess is not created also its modular programing and every file has seprate functionality so its seprated 
What does import do?
import is used to import the module to the program
What is the purpose of main.py?
it is the main python file that holds main operations or core code
Why shouldn't all code be written inside one file?
because its not reliable and not easy to manage and maintain also it create a mess

📚 New Technical Terms

Write definitions in your own words.

Module
A module is a file contain code that is resuable
Import
used to import file as module 
from
used to import specific function of the module
Entry Point
the file where application starts
__name__
__main__
used to revent execution of the module directly
Code Reusability
make the code used again and again without any difficullty
Separation of Concerns
every file have different function so sepration of concerns here means we should be concerned for every file differently
Business Logic
to prevent creating mess and used clean relaibale and readable easy to remove bugs code
Application Flow
the flow of the execution from entry poit to the execution




i would use option b because every file has seprate functionality so its seprated  Developers split projects into multiple files to make it clean and easy to maintain so a mess is not created  